# players
players["p_1"] = Player("P1", 1280 / 2-1, 720 / 2, "red",
	pygame.K_a, pygame.K_d, pygame.K_w, pygame.K_s, pygame.K_LCTRL)
players["p_2"] = Player("P2", 1280 / 2+1, 720 / 2, "blue",
	pygame.K_LEFT, pygame.K_RIGHT, pygame.K_UP, pygame.K_DOWN, pygame.K_RETURN)

# objects
for i in range(5):
	angle = i * 30
	test = 370
	starting_point = pygame.math.Vector2(1280 / 2 + test * math.sin(math.radians(angle)), 720 / 2 + test * math.cos(math.radians(angle)))
	Rect((0, 0, angle / 2), starting_point, 200, 10,
	facing = angle,
	rotation = ((1280 / 2, 720 / 2), 1, -1))

for i in range(6, 11):
	angle = i * 30
	test = 370
	starting_point = pygame.math.Vector2(1280 / 2 + test * math.sin(math.radians(angle)), 720 / 2 + test * math.cos(math.radians(angle)))
	Rect((0, 0, angle / 2), starting_point, 200, 10,
	facing = angle,
	rotation = ((1280 / 2, 720 / 2), 1, -1))

for i in range(2, 4):
	angle = i * 60
	test = 100
	starting_point = pygame.math.Vector2(1280 / 2 + test * math.sin(math.radians(angle)), 720 / 2 + test * math.cos(math.radians(angle)))
	Rect((255-angle/2, 0, 0), starting_point, 120, 10,
	facing = angle,
	rotation = ((1280 / 2, 720 / 2), -2, 2))

for i in range(5, 7):
	angle = i * 60
	test = 100
	starting_point = pygame.math.Vector2(1280 / 2 + test * math.sin(math.radians(angle)), 720 / 2 + test * math.cos(math.radians(angle)))
	Rect((255-angle/2, 0, 0), starting_point, 120, 10,
	facing = angle,
	rotation = ((1280 / 2, 720 / 2), -2, 2))
